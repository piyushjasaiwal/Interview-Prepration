/*
 * 1. You are given two numbers which represent the numerator and denominator of a fraction.
2. You have to convert this fraction into a decimal.
3. If the decimals are repeating recursively, then you have to put the recurring sequence inside a bracket.
Input Format
A number N1 representing the numerator of a fraction.
A number N2 representing the denominator of a fraction.
Output Format
A String representing a decimal number. 
Question Video

  COMMENTConstraints
1 <= N1,N2 <= 2000 
Sample Input
8
4
Sample Output
2
 */

import java.util.*;

class recurring_sequence_in_a_fraction {

    public static String solution(int num, int den) {
        // write your code here
        
        return null;
    }
    
    public static void main(String[] args) {
		Scanner scn = new Scanner(System.in);
		int num = scn.nextInt();
		int den = scn.nextInt();
		System.out.println(solution(num , den));
	}

}
